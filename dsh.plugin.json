{
  "id": "dsh-session-theme",
  "version": "0.2.1",
  "main": "./lib/index.js",
  "description": "DSH web plugin: show every session's theme in the left sidebar on load. Warms the session projection cache at startup so session.list carries title projections for cold sessions.",
  "engines": {
    "dsh": ">=0.1.0"
  },
  "contributes": {
    "tools": [],
    "skills": []
  }
}
